inteiro fact(inteiro k);

vazio principal(vazio) {
    inteiro n;
    escrevers("Introduza inteiro: ");
    n = ler();
    escrever(fact(n));
}

inteiro fact(inteiro k) {
    se (k <= 1) {
        retornar 1;
    } senao {
        retornar k * fact(k - 1);
    }
}